# Images are source assets and should not be ignored
# No build artifacts, dependencies, or temp files detected in changes
# Only preserving existing rules if any, but none provided

# No rules needed for this project based on the file changes shown